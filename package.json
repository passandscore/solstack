{
  "name": "solstack",
  "version": "1.0.0",
  "description": "A collection of smart contract examples.",
  "scripts": {
    "test": "forge test",
    "coverage": "yarn && forge coverage --report lcov && lcov --remove lcov.info 'test/*.sol'  -o lcov_filtered.info && genhtml lcov_filtered.info --branch-coverage --output-dir coverage --ignore-errors category && rm lcov.info && rm lcov_filtered.info && open coverage/index.html"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/passandscore/smart-contracts.git"
  }
}
